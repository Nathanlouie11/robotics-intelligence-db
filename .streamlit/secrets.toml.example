# Streamlit Secrets Configuration
# Copy this to secrets.toml and fill in your values
# For Streamlit Cloud: Add these in the app settings > Secrets

# Optional: External LLM API (for cloud deployment where Ollama isn't available)
# [openai]
# api_key = "sk-..."

# [anthropic]
# api_key = "sk-ant-..."

# Optional: Custom Ollama host (if running Ollama on a remote server)
# [ollama]
# host = "http://your-ollama-server:11434"
# model = "qwen2.5:32b"
